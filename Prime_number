elements = list(range(2, num))
result = 'это простое число'
for x in elements:
    if num % x == 0:
        result = 'это не простое число'
        break
    else:
        result = 'это простое число'
